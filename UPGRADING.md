# 升級這個 skill（給被叫來做這件事的 AI 看）

使用者把這個 repo 的網址貼給你，大致是這樣：

```
更新這個 skill，然後幫我升級 <我的分析資料夾>
https://github.com/tzuweii/netlist-deep-dive
```

這份文件就是完整的做法。**照順序做完，不要跳步驟。**

這份流程是**版本無關**的 —— 不管使用者手上是哪一版、要升到哪一版，做法都一樣。
版本特有的事情（例如某一版開始需要新的輸入檔）寫在 `CHANGELOG.md` 每個版本的
**「升級」**小節，第 3 步會叫你去讀。

---

## 0. 先問清楚：分析資料夾在哪

使用者常常只說「幫我升級」。**沒拿到路徑就先問**，不要自己猜、不要去掃磁碟。

如果使用者說「我還沒建過專案」，那不是升級，是 `init`，直接跳到 `SKILL.md`
的 Phase 0。

## 1. 找到 skill 裝在哪

```bash
python <目前這份 skill>/scripts/ndd.py version
```

印出的「安裝位置」就是要更新的目錄。**如果你找不到既有安裝**（使用者是第一次
裝），clone 一份：

```bash
git clone https://github.com/tzuweii/netlist-deep-dive .claude/skills/netlist-deep-dive
```

⚠️ **不要在分析資料夾裡 clone。** skill 跟分析資料是分開的兩個東西，混在一起
之後 `.gitignore` 會開始吃掉使用者的產出。

## 2. 更新

```bash
cd <上一步的安裝位置>
git pull
```

不是 git 工作區（壓縮檔或手動複製裝的，`version` 會告訴你）就重新 clone 一份
取代舊目錄 —— **skill 本身不保存任何專案資料，砍掉重裝是安全的**。

跑一次測試確認這份程式在這台機器上是好的：

```bash
python -m unittest discover -s tests
```

**沒有 OK 就停下來回報，不要繼續升級使用者的專案。**

## 3. 讀 CHANGELOG 的「升級」小節

```bash
python scripts/ndd.py version "<分析資料夾>"
```

它會印出這個專案是哪一版建的、缺什麼、下一步該下哪個指令。

然後打開 `CHANGELOG.md`，**把「那個專案的版本」到「現在這一版」之間每一個版本的
「升級」小節都讀過**。沒有那個小節的版本代表**不需要額外動作**，跳過就好。

新的大版本常常需要**新的輸入檔**：

| 版本 | 那一版開始需要 |
|---|---|
| v2.0.0 | 每塊板多一份 `.DSN`（OrCAD Capture 設計檔），以及這台機器要裝 Capture |

## 4. 缺什麼就先跟使用者要

如果上一步發現缺輸入檔（例如 v2.0 需要 `.DSN`），**先向使用者要，放進原本那個
分析資料夾**，再往下走。

要得到就照要；要不到也可以升級 —— 工具會照常升完，只是少掉那一版新增的能力，
並在報告裡列出缺哪幾塊板。**不要因為缺檔就停下來什麼都不做。**

## 5. 升級專案

```bash
python scripts/ndd.py migrate "<分析資料夾>" --run
```

> ### ⚠️ 絕對不要對既有資料夾跑 `init`
>
> `init` 會**覆蓋** `ndd.json`：手寫的斷言、命名規則、對接關係、封裝指定全部
> 消失（實測一個真實專案：63 條斷言、12 條正規化規則、16 組對接）。
> 工具會擋下來並指向 `migrate`，但**不要靠工具擋** —— 看到資料夾裡有 `ndd.json`
> 就該直接用 `migrate`。
>
> `migrate` 只會 `update` 新欄位，不刪不覆寫任何手寫設定，而且原檔會另存備份。

`migrate --run` 會升級設定、補上新版需要的東西、重跑所有流程，並把結果寫進
分析資料夾的 `SETUP.md`。

## 6. 讀升級報告，把沒做完的講出來

```
<分析資料夾>/SETUP.md
```

**「跑完了」不等於「都成功了」。** 報告裡會分開列出「做到的」與「沒做到的」。
沒做到的一定要原文轉述給使用者，不要只說「升級完成」。

最常見的兩種：

- **某塊板的新輸入檔與 `.asc` 對不上** —— 那塊板不會被寫進設定（維持原本行為），
  報告會帶完整細節（哪條 net、哪顆零件對不上）。這通常是**檔案版本不同**，
  請使用者確認是不是同一塊板的同一版。
- **同料號的資料互相矛盾** —— 工具**拒絕替使用者挑一個**，整筆不寫入並列出來。

## 7. 回報

一句話講清楚這三件事：

1. skill 從哪一版更新到哪一版
2. 專案升級後多了什麼能力（看 CHANGELOG 那一版的開頭）
3. **還有什麼要使用者處理** —— 逐條列出，不要概括成「有一些待辦」

---

## 這份流程的設計原則

**判斷該做什麼一律看實際檔案，不是看版本戳。** `ndd.json` 裡的 `ndd_version`
只是給人看的說明；戳記會因為手動編輯、複製資料夾、中途失敗而說謊，「那幾份
檔案到底在不在」不會。所以 `migrate` 重跑不會壞事，也不會重做已經做完的事。

**升級失敗要保住既有專案。** 新版的某個輸入對不上時，`migrate` 讓那塊板維持
原本行為，而不是帶著錯的資料繼續跑 —— 看得見的缺席優於看不見的錯誤。
（`init` 剛好相反：那時候還沒有東西存在，停下來零成本。）

---

## 給維護者：發新版時要做的

要讓上面這套流程在未來每一版都成立，發版時：

1. `VERSION` 改掉。
2. `CHANGELOG.md` 新增該版區段，**一定要有 `### 升級` 小節**，寫清楚：
   - 需不需要新的輸入檔／新的外部相依
   - `migrate` 會自動做什麼、不會做什麼
   - 缺東西時會怎樣（照跑？中止？）
3. 如果需要新的輸入檔，把它加進本文第 3 步的表格。
4. 讓 `migrate` 能**就地**完成升級，而且對舊專案是無害的 no-op。
   新版的升級邏輯要能從實際檔案判斷該不該做，不要依賴版本戳。
5. 跑 `python -m unittest discover -s tests`，並實際對一個舊版專案跑一次
   `migrate --run`。**合成測試照不到的東西，只有真的跑一次才看得到。**
